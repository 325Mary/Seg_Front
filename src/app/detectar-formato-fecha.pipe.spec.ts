import { DetectarFormatoFechaPipe } from './detectar-formato-fecha.pipe';

describe('DetectarFormatoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new DetectarFormatoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
